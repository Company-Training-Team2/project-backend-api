{
  "_comment": "Real secrets (DB password, JWT signing key) are NOT in this file — it's committed to a public repo. They live in appsettings.Production.json (gitignored, exists only on the machine that publishes) which ASP.NET Core layers on top of this file automatically when ASPNETCORE_ENVIRONMENT=Production. See back-end/README.md.",

  "ConnectionStrings": {
    "DefaultConnection": "Server=(set in appsettings.Production.json, not committed);Database=db62877;User Id=db62877;Password=(set in appsettings.Production.json);Encrypt=False;MultipleActiveResultSets=True;TrustServerCertificate=True"
  },

  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000",
      "https://project-web-frontend-two.vercel.app",
      "https://project-web-frontend-git-main-aysha-kassems-projects.vercel.app"
    ]
  },

  "Jwt": {
    "Secret": "(set in appsettings.Production.json, not committed — must be 32+ chars)",
    "Issuer": "EventHub",
    "Audience": "EventHubClient",
    "ExpiryMinutes": "60"
  },

  "Smtp": {
    "Host": "smtp.gmail.com",
    "Port": "587",
    "Username": "",
    "Password": "",
    "FromEmail": "noreply@eventhub.com"
  },

  "Paymob": {
    "BaseUrl": "https://accept.paymob.com",
    "ApiKey": "",
    "HmacSecret": "",
    "IntegrationId": "",
    "IframeId": ""
  },

  "App": {
    "Name": "EventHub"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*"
}
